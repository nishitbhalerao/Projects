<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Blockchain Student Registration</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #1c1c1c; /* black */
      color: white;
      margin: 0;
      padding: 0;
    }
    header {
      background: linear-gradient(90deg, #ffcc00, #ff3333); /* yellow to red */
      color: black;
      text-align: center;
      padding: 20px;
      font-size: 26px;
      font-weight: bold;
      letter-spacing: 1px;
    }
    .container {
      max-width: 900px;
      margin: 30px auto;
      background: #2e2e2e; /* grey */
      padding: 25px;
      border-radius: 15px;
      box-shadow: 0 0 15px rgba(255,255,255,0.2);
    }
    h2 {
      text-align: center;
      color: #ffcc00;
      margin-bottom: 20px;
    }
    form {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 15px;
    }
    label {
      font-weight: bold;
      margin-bottom: 5px;
      display: block;
    }
    input {
      width: 95%;
      padding: 8px;
      border: none;
      border-radius: 6px;
      outline: none;
    }
    .btn {
      grid-column: span 2;
      background: #ff3333; /* red */
      color: white;
      padding: 12px;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      transition: 0.3s;
    }
    .btn:hover {
      background: #ffcc00;
      color: black;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 25px;
      text-align: center;
    }
    th, td {
      border: 1px solid #555;
      padding: 10px;
    }
    th {
      background: #ffcc00;
      color: black;
    }
    tr:nth-child(even) {
      background: #3a3a3a;
    }
    tr:nth-child(odd) {
      background: #2a2a2a;
    }
  </style>
</head>
<body>

  <header>🚀 Blockchain Student Registration</header>

  <div class="container">
    <h2>Register New Student</h2>
    <form id="registerForm">
      <div>
        <label for="name">Student Name</label>
        <input type="text" id="name" required>
      </div>
      <div>
        <label for="gmail">Gmail</label>
        <input type="email" id="gmail" required>
      </div>
      <div>
        <label for="phone">Phone Number</label>
        <input type="text" id="phone" required>
      </div>
      <div>
        <label for="city">City</label>
        <input type="text" id="city" required>
      </div>
      <div>
        <label for="state">State</label>
        <input type="text" id="state" required>
      </div>
      <button type="submit" class="btn">Register</button>
    </form>

    <h2>📋 Registered Students</h2>
    <table id="studentsTable">
      <thead>
        <tr>
          <th>Name</th>
          <th>Gmail</th>
          <th>Phone</th>
          <th>City</th>
          <th>State</th>
        </tr>
      </thead>
      <tbody>
        <!-- Students will appear here -->
      </tbody>
    </table>
  </div>

  <script>
    const form = document.getElementById("registerForm");
    const tableBody = document.querySelector("#studentsTable tbody");

    form.addEventListener("submit", function(event) {
      event.preventDefault();

      const name = document.getElementById("name").value;
      const gmail = document.getElementById("gmail").value;
      const phone = document.getElementById("phone").value;
      const city = document.getElementById("city").value;
      const state = document.getElementById("state").value;

      const newRow = document.createElement("tr");

      newRow.innerHTML = `
        <td>${name}</td>
        <td>${gmail}</td>
        <td>${phone}</td>
        <td>${city}</td>
        <td>${state}</td>
      `;

      tableBody.appendChild(newRow);

      form.reset();
    });
  </script>

</body>
</html>
